import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.worker.starter.app',
  appName: 'capacitor-wasm-worker-starter',
  webDir: 'dist'
};

export default config;
